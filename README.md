# pyAssignment2
TASK 1: takes 'a' as an integer input and checks with if statement, if (a%2==0,i.e a divisible by 2) its true then the print statement is executed and if its false then the else statement is executed.
TASK 2: initialized a as 0, then used for loop and set the range to 1,51 (till 50), then summation is done(x+=a,i.e,x=x+a). After summation, the result is printed using print statement.
